apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: pkrb-nprod-gateway-internal
  namespace: default
spec:
  selector:
    istio: pkrb-dev-internal
  servers:
  - hosts:
    - '*'
    port:
      name: http
      number: 80
      protocol: HTTP
