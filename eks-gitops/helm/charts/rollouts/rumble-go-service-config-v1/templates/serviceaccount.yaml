{{- if .Values.serviceAccount.iam_role }}
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: {{ .Values.container.name }}
    env: {{ .Values.env }}
  name: {{ .Values.container.name }}-sa
  namespace: {{ .Values.env }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.serviceAccount.iam_role }}
---
{{- end }}
